# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Netlify CI artifacts — must NOT be committed (Linux CI regenerates per build;
# committing causes EINTEGRITY plugin-install errors and Windows-path leaks)
.netlify/

# Local creative assets (videos, images for ad uploads — not source)
creative/

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?
