# ── Client (browser) env — Vite exposes only VITE_-prefixed vars ──────────────
# Copy to .env.local for dev; set the same in the Netlify build environment.
# Anon key is safe to ship to the browser (RLS protects every table).
VITE_SUPABASE_URL=https://dwsmrruzufqpopdzlszw.supabase.co
VITE_SUPABASE_ANON_KEY=your-supabase-anon-key

# ── Local scripts only (NOT bundled) — kept in .env.local ─────────────────────
# META_ADS_TOKEN=...
# META_AD_ACCOUNT_ID=act_553999801104558
# META_DATASET_ID=4230021860577001
